import { loadCommonElements } from './common.js';

// --- Initialization ---
document.addEventListener('DOMContentLoaded', () => {
    loadCommonElements();
    
    // All Firebase & form submission logic has been temporarily removed 
    // to allow for merging stable code.
    // The HTML page now uses a 'mailto:' link as a fallback.
});
